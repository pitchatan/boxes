/*

//void(string shadername, vector origin, vector up, vector side, vector rgb, float alpha) adddecal = #375; 

	makevectors(view_angles);
	traceline(view_origin,view_origin + (v_forward * 2000),0,self);
	adddecal("outlines",trace_endpos,v_up,v_right * 2000,[1,1,1],1);
	
	
*/

